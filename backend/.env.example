NODE_ENV=development
PORT=3000

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=halladmin
DB_PASSWORD=change-me
DB_NAME=hall
DB_POOL_SIZE=10

# 生产环境必须设置为高强度随机字符串，例如：openssl rand -hex 32
JWT_SECRET=change-me-to-a-long-random-secret
JWT_EXPIRES_IN=8h
